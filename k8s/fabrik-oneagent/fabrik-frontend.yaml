apiVersion: apps/v1
kind: Deployment
metadata:
  name: fabrik-frontend
  namespace: fabrik-oneagent
  labels:
    app: fabrik-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fabrik-frontend
  template:
    metadata:
      labels:
        app: fabrik-frontend
      annotations:
        dynatrace.com/inject: "true"
    spec:
      containers:
      - name: fabrik-frontend
        image: ghcr.io/mreider/fabrik-frontend-oneagent:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        env:
        - name: DT_SERVICE_NAME
          value: "fabrik-frontend-oneagent"
        - name: FABRIK_SERVICE_URL
          value: "http://fabrik-service.fabrik-oneagent:8080"
        - name: FABRIK_PROXY_URL
          value: "http://fabrik-proxy.fabrik-oneagent:8080"
        - name: LOAD_GENERATOR_ENABLED
          value: "true"
        - name: LOAD_INTERVAL_MIN
          value: "3"
        - name: LOAD_INTERVAL_MAX
          value: "8"
        # Disable OpenTelemetry for OneAgent-only setup
        - name: OTEL_SDK_DISABLED
          value: "true"
        resources:
          limits:
            cpu: "300m"
            memory: "256Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: fabrik-frontend
  namespace: fabrik-oneagent
spec:
  selector:
    app: fabrik-frontend
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP